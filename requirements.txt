# Python packages installed into the platform venv.
# Gazebo transport/messages and PyGObject/GStreamer bindings are provided by
# system packages on the target host and are checked separately by deploy tools.
mavsdk
websockets
