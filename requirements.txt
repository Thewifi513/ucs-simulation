# Python packages installed into the platform venv.
# Gazebo transport/messages and PyGObject/GStreamer bindings may be provided by
# the ucs-gazebo-runtime Docker helper on Ubuntu 20 deployments.
mavsdk
websockets
